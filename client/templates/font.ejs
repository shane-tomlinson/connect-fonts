@font-face {
  font-family: '<%= fontFamily %>';
  font-style: <%= fontStyle %>;
  font-weight: <%= fontWeight %>;
  <% formats.forEach(function(format, index) { if(format.type === "embedded-opentype") { %>src: url('<%= cachify.cachify(format.location) %>'); <% } }) %>
  src: <% formats.forEach(function(format, index) { %><%- partial("format", { format: format.type, location: format.location, index: index, total: formats.length }) %> <% }); %>
}

