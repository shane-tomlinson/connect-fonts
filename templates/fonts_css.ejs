/* Generated by shane's awesome program */
<% var total = fonts.length; fonts.forEach(function(font) { %>@font-face {
  font-family: '<%= font.fontFamily %>';
  font-style: <%= font.fontStyle %>;
  font-weight: <%= font.fontWeight %>;
  <% font.formats.forEach(function(format, index) { if(format.type === "embedded-opentype") { %>src: url('<%= (format.location) %>'); <% } }) %>
  src: <% font.formats.forEach(function(format, index) { if(index !== 0) { %> <% } if(format.type === "local") { %>local('<%= format.location %>')<%= index === (total - 1) ? ";" : "," %> <% } else { %>url('<%= (format.location) %>') format('<%= format.type %>')<%= index === (total - 1) ? ";" : "," %> <% } }); %>
}<% }) %>
